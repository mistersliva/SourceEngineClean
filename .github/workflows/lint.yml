name: Lint

on: [push, pull_request]

jobs:
  lint-legacy-code:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Legacy-code ratchet lint
      shell: pwsh
      run: |
        ./scripts/lint-legacy.ps1
